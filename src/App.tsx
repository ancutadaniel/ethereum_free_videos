// App.tsx
import React from "react";
import Header from "./components/Header/Header";

const App: React.FC = () => {
  return (
    <main>
      <Header />
      {/* remaining components */}
    </main>
  );
};

export default App;
